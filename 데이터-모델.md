# 데이터 모델

이 문서가 진실의 원본이다. `data/*.toml`과 `tools/build.py`는 여기에 맞춘다.

## 왜 데이터가 먼저인가

만들려는 것이 셋(기기 탐색 / 인쇄물 / 대화 응답)이고, 셋 다 같은 것을 요구한다 —
**인물·사건·장소·시대와 그 관계가 기계가 읽을 수 있는 형태로 정리된 것.**
웹앱을 먼저 만들면 데이터가 코드와 뷰에 갇혀 인쇄물로 못 나오고, 문서를 먼저 쓰면
관계(계보·인과·이동)가 문장 안에 녹아 검색·연결이 안 된다.

그래서 `data/` 하나 → `tools/render_*.py` 여럿. 렌더러는 데이터를 읽기만 한다.

## 형식: TOML

- 표준 라이브러리 `tomllib`로 읽는다. **의존성 0.** (PyYAML 없음)
- `"""` 여러 줄 문자열이 있어 한국어 본문을 그대로 쓴다.
- `#` 주석으로 집필 메모·출처 확인 흔적을 파일에 남긴다.
- 항목당 한 파일이 아니라 **묶음당 한 파일**(`data/figures/olympians.toml`). 300항목에서
  파일 300개는 편집·검색이 더 불편하다. 묶음은 시대·계열 기준.

## 세 개의 축, 세 개의 모델링 난제

### 1. 시간 — 연대가 없다

신화에는 절대 연대가 없다. 대신 원전이 실제로 쓰는 것은 **세대**다
(헤라클레스의 아들 틀레폴레모스가 트로이에서 싸우고, 아이아스의 아버지 텔라몬은
헤라클레스와 함께 항해했다 — 원전이 세대를 명시한다).

→ `era` (0~8 신화 시대) + `seq` (시대 안의 정렬키). 절대 연대 필드는 두지 않는다.
아이가 "언제야?"라고 물으면 "몇 번째 세대"로 답한다.

| era | 이름 | 내용 |
|---|---|---|
| 0 | 태초 | 카오스에서 첫 신들이 나오다 |
| 1 | 티탄의 시대 | 우라노스와 크로노스의 통치 |
| 2 | 올림포스의 시대 | 제우스가 세계를 나누어 다스리다 |
| 3 | 사람의 시대가 열리다 | 프로메테우스, 판도라, 큰 홍수 |
| 4 | 첫 영웅들 | 페르세우스, 벨레로폰, 크레타의 미노스 |
| 5 | 큰 영웅들 | 헤라클레스, 테세우스, 아르고호 |
| 6 | 트로이 전쟁 | 10년의 전쟁 |
| 7 | 돌아오는 길 | 오디세우스, 오레스테스, 아이네이아스 |
| 8 | 로마가 세워지다 | 알바 롱가에서 로물루스까지, 로마의 신들 |

### 2. 장소 — 실제 땅과 신화 속 세계가 섞여 있다

델포이는 위도·경도가 있고 지금 가면 있다. 타르타로스는 없다. 한 지도에 섞으면
아이에게 거짓을 가르친다.

→ `kind = "real"`은 `lat`/`lon`/`modern`(현대 지명)을 갖고 **실제 지도**에 놓는다.
`kind = "mythic"`은 `layer`(sky/earth/sea/underworld/edge)와 도해 좌표 `cx`/`cy`를 갖고
**우주 도해**(하늘–땅–바다–지하 층)에 놓는다. 두 뷰는 분리한다.

### 3. 이름 — 하나가 셋이다

제우스=유피테르=주피터. 아이는 셋 중 무엇으로든 묻는다.

→ `name_ko`(주 표기) / `name_grc`(그리스명 라틴 전사) / `name_la`(로마명) / `aka[]`(이형·별칭·다른 한글 표기).
검색은 이 전부에 걸린다. 8~10세 본문에서는 `name_ko` 하나만 쓰고, 로마명은
"로마에서는 ○○라고 불렀어" 한 줄로만 준다.

### 4. 이설(異說) — 원전끼리 다르다

아프로디테는 우라노스의 거품에서 났고(헤시오도스), 제우스와 디오네의 딸이다(호메로스).
둘 다 진짜다. 하나를 골라 감추면 나중에 아이가 다른 책을 보고 배신감을 느낀다.

→ 주 서술은 하나 고르고, `variants[]`에 `{source, text}`로 다른 판본을 남긴다.
렌더러는 "다른 이야기도 있어" 박스로 보여준다. 계보에 이설이 걸리면 `parents_variant`.

### 5. 수위 — 원전 상당수는 8~10세에 그대로 줄 수 없다

그리스 신화 원전에는 성폭력, 근친, 유아살해, 식인이 흔하다. 빼면 이야기가 무너지는
경우(크로노스가 자식을 삼킴 — 이건 그대로 준다)와, 빼도 되는 경우(제우스의 겁탈 다수)를
구분해야 한다. 판단 기준은 [집필-지침.md](집필-지침.md)에 두고, 항목에는 표시만 남긴다.

→ `sensitivity = "none" | "soften"`. `omit` 판정은 데이터에 넣지 않는다(존재하지 않음).
`soften`인 항목은 지침의 완화 규칙을 적용해 집필하고, 대화 에이전트는 아이가 더 물으면
"그건 어른이 되면 읽을 이야기야"로 멈춘다.

### 6. 구간 — 사건은 점이 아니다

트로이 전쟁은 열 해이고 원반이 맞은 것은 한 순간이다. 우라노스가 자식을 가둔 상태는
티타노마키아가 그것을 끝낼 때까지 이어진다. 어떤 사건은 다른 사건을 품고, 어떤 것은 겹친다.
그래서 사건은 점이 아니라 **눈금 없는 하나의 축 위의 구간**이다.

**좌표는 데이터에 적지 않는다.** 적어 두면 인과를 하나 고칠 때마다 어긋나고, 그 어긋남은
조용하다. 데이터에는 관계만 적고, `tools/build.py` 의 `solve_axis()` 가 **가장 긴 경로**로
좌표를 푼다(PERT 와 같은 방식). 인과에 순환이 있으면 = 모순이면 그 자리에서 죽는다.

제약은 이것뿐이다.

| 근거 | 제약 |
|---|---|
| 시대 경계 | `E_n < E_(n+1)` |
| 사건은 자기 시대에서 **시작**한다 | `E_n ≤ t0 < E_(n+1)` |
| 길이 등급의 최소값 | `t1 ≥ t0 + SPANS[span]` |
| 같은 시대 안에서 `seq` 는 **시작 순서** | `t0(작은 seq) < t0(큰 seq)` |
| `caused_by` — 원인이 먼저 시작한다 | `t0(원인) < t0(결과)` |
| `after` — 완전히 끝난 뒤에 시작한다 | `t1(앞) ≤ t0(뒤)` |
| `within` — 바깥이 안을 감싼다 | `t0(밖) ≤ t0(안)`, `t1(안) ≤ t1(밖)` |
| 끝이 붙잡히지 않았고 `open` 도 아니면 자기 시대를 넘지 못한다 | `t1 ≤ E_(n+1)` |

마지막 줄이 중요하다. 길이 등급 하나 때문에 막대가 다음 시대까지 뻗으면, 그림이
**데이터에 없는 말**("이 일이 다음 시대까지 이어졌다")을 하게 된다. 끝이 시대를 넘는 것은
두 경우뿐이다 — 뒤의 사건이 `after` 로 끝을 붙잡고 있거나(우라노스의 감금 → 티타노마키아가 끝낸다),
`open = true` 로 "끝나지 않거나 끝을 아직 모른다"고 표시한 것(계절의 되풀이, 프로메테우스의 벌).

`span` 은 실제 길이가 아니라 등급이다. 신화에 정확한 길이는 없다.

| `span` | 뜻 | 축에서의 최소 길이 |
|---|---|---|
| `moment` | 한 순간 | 1 |
| `days` | 며칠 | 2 |
| `season` | 한 철 | 4 |
| `years` | 여러 해 | 9 |
| `age` | 한 세대 넘게 | 24 |

화면에서도 이 사실을 숨기지 않는다. 축에 눈금을 그리지 않고, "막대 길이는 실제 길이가 아니라
한 순간과 여러 해를 구별하는 정도"라고 적어 둔다. `open` 인 막대는 오른쪽으로 점선이 이어진다.

**아직 정직하지 않은 곳:** 같은 시대 안의 시작 순서는 `seq`, 곧 원전이 이야기하는 순서다.
원전이 순서를 말하지 않는 짝(아테네 경합과 페르세포네 납치)도 이 축에서는 순서가 생긴다.
"모른다"를 표시할 방법이 아직 없다.

그런 짝을 빌드가 뽑아 [`build/불확실-점검.md`](build/불확실-점검.md) 에 적는다
(`caused_by`/`after` 로 이어지는 길이 없는 `seq` 이웃). 판단과 처리 방침은
[불확실-목록.md](불확실-목록.md) 에 있다.

era 5~8 이 들어오자 이 짝이 5건에서 48건으로 뛰었고, 들여다보니 **성질이 다른 두 가지가
섞여 있었다.** 그래서 점검을 둘로 갈랐다.

| 종류 | 뜻 | 어떻게 볼 것인가 |
|---|---|---|
| 같은 묶음 안 | 한 이야기 안의 두 사건인데 이어 줄 관계가 없다 | **고칠 것일 수 있다.** `caused_by`/`after` 가 빠진 것인지 본다 |
| 다른 묶음 사이 | 한 시대에 든 두 이야기의 이음매 | **정상이다.** 원전이 순서를 말하지 않는 것이 맞다. 나란한 이야기로 읽어야 한다 |

한 시대에 여러 이야기가 들어 있으면 이음매는 반드시 생긴다(era 4 에 페르세우스·에우로페·
벨레로폰·카드모스·탄탈로스가 함께 있다). 그것을 "모르는 순서"로 세면 목록이 잡음에 묻힌다.

---

## 스키마

### 공통

| 필드 | 필수 | 뜻 |
|---|---|---|
| `id` | ✓ | ASCII kebab-case. **절대 바꾸지 않는다** (링크 대상) |
| `name_ko` | ✓ | 주 표기 |
| `aka` | | 이형·별칭·다른 한글 표기 (검색용) |
| `oneliner` | ✓ | 한 문장. 카드·툴팁·검색 결과에 쓰인다 |
| `body` | ✓ | 3~5문장. 8~10세 대상 본문 |
| `fun` | | "재밌는 것 하나". 아이를 끌어들이는 미끼 |
| `sources` | ✓ | `["hesiod.theogony 116-138", ...]` — 원전 id + 위치 |
| `sensitivity` | | `none`(기본) \| `soften` |
| `note` | | 집필 메모. 렌더링 안 됨 |

### `data/figures/*.toml` — `[[figure]]`

| 필드 | 필수 | 뜻 |
|---|---|---|
| `name_grc` | | 그리스명 라틴 전사 (`Zeus`) |
| `name_la` | | 로마명 (`Iuppiter`) |
| `kind` | ✓ | `primordial` \| `titan` \| `god` \| `hero` \| `human` \| `monster` \| `nymph` \| `group` |
| `era` | ✓ | 처음 등장하는 시대 |
| `parents` | | figure id 0~2개 |
| `parents_variant` | | 이설 계보 `[{source, parents}]`. `text` 를 더 쓸 수 있다 — 아이가 읽을 한 문장. 없으면 렌더러가 `parents` 로 만든다 |
| `spouses` | | figure id |
| `domains` | | 담당 영역 (`["하늘", "번개"]`) |
| `symbols` | | 상징물 (`["번개", "독수리"]`) |
| `home` | | place id |

`children`은 **두지 않는다.** `parents`의 역인덱스로 빌드가 만든다 (중복 = 불일치의 원천).
같은 이유로 사건의 `arc`도 두지 않는다. `arcs.toml`의 `events` 목록이 유일한 출처이고
빌드가 역인덱스로 `event.arc`를 만든다. 한 사건이 두 묶음에 들어가면 에러다.

### `data/events/*.toml` — `[[event]]`

| 필드 | 필수 | 뜻 |
|---|---|---|
| `era` | ✓ | 시대 |
| `seq` | ✓ | 시대 안의 정렬키 (정수, 10 간격으로 띄워 둔다 — 나중에 끼워넣기) |
| `place` | | 주 장소 place id |
| `places` | | 여러 곳을 이동하는 사건 (순서대로) |
| `cast` | ✓ | `[{figure, role}]` — role: `주인공` \| `상대` \| `도움` \| `피해` \| `등장` |
| `caused_by` | | 앞선 event id (인과) |
| `variants` | | `[{source, text}]` |
| `span` | | 길이 등급 `moment`\|`days`\|`season`\|`years`\|`age` (기본 `moment`) |
| `after` | | 이 사건이 **완전히 끝난 뒤에** 시작한다 — event id |
| `within` | | 이 사건을 품는 사건 — event id |
| `open` | | `true` 면 끝나지 않거나 끝을 아직 모른다 |

### `data/places/*.toml` — `[[place]]`

| 필드 | 필수 | 뜻 |
|---|---|---|
| `kind` | ✓ | `real` \| `mythic` |
| `lat`, `lon` | real ✓ | 실제 좌표 |
| `modern` | real ✓ | 현대 지명 (`"그리스 델피"`) |
| `layer` | mythic ✓ | `sky` \| `earth` \| `sea` \| `underworld` \| `edge` |
| `cx`, `cy` | mythic ✓ | 우주 도해 좌표 0~1 |

### `data/arcs.toml` — `[[arc]]`

| 필드 | 필수 | 뜻 |
|---|---|---|
| `era` | ✓ | 시대 |
| `events` | ✓ | event id, 이야기 순서대로 |

### `data/sources.toml` — `[[source]]`

| 필드 | 필수 | 뜻 |
|---|---|---|
| `id` | ✓ | `hesiod.theogony` |
| `author_ko`, `title_ko` | ✓ | `헤시오도스`, `신들의 계보` |
| `title_orig` | ✓ | `Theogony` |
| `written` | ✓ | `기원전 700년쯤` |
| `file` | | `sources/` 안의 파일 (있으면) |
| `translator` | | 확보한 번역본의 번역자 |

## 빌드

```
data/*.toml  --tools/build.py-->  build/myth.json
                (검증 + children·arc 역인덱스 + 시간축 구간 풀기 + 정렬)
                                        |
              +-------------------------+-------------------------+
              |                         |                         |
       render_web.py            render_print.py           render_agent.py
              |                         |                         |
      build/myth.html          build/print-*.html         build/agent-pack/
    (단일 파일, 오프라인)      (A3 인쇄용, 브라우저 인쇄)   (my-talking-claw용 지침+지식)
```

`build.py`는 **깨지면 즉시 죽는다.** 방어 코드·기본값 채우기 없음.
없는 id를 참조하면 그 자리에서 에러. 데이터가 틀린 채로 산출물이 나오는 것이 최악이다.
