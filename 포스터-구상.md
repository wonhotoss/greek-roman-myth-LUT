# 인포그래픽 포스터 — 넷째 축의 조사와 구상

2026-09-03 에 **인포그래픽 포스터를 산출물의 넷째 축으로 정했다.** 벽에 붙여 두는 한 장이다.
아직 렌더러는 없다. 이 문서는 그날 한 조사와 구상안, 진행 단계를 적은 것이다.
스키마는 [데이터-모델.md](데이터-모델.md), 문장·수위 규칙은 [집필-지침.md](집필-지침.md) 를 그대로 따른다.

## 왜 넷째 축인가, 어디에 놓이는가

앞의 셋(화면·인쇄물·음성)은 모두 **찾아보는** 것이다. 포스터는 **붙여 두고 눈에 익히는** 것이라 용도가 다르다.
스트리트H 포스터처럼 한 주제를 한 장에 담는다. 아이가 자기 방 벽에서 매일 보는 것이 목표다.

자리는 다른 셋과 같다.

```
data/*.toml ──build.py──▶ build/myth.json ──render_poster.py──▶ build/poster-<id>.pdf (+ png 미리보기)
data/posters/<id>.toml ─┘   (무엇을 넣고 어디에 놓을지)
```

규칙은 [HANDOFF.md](HANDOFF.md) 의 여덟 가지 그대로다. 특히 **렌더러는 JSON 만 읽는다.** 포스터에 들어가는
글은 항목의 `oneliner`·`fun`·`modern` 이고, 포스터용 새 문장을 렌더러 안에 쓰지 않는다. 어느 항목을 넣고
어디에 놓을지는 `data/posters/<id>.toml` 에 적는다(아래 "데이터·코드 제안").

## 참고 모델 — 스트리트H 인포그래픽 포스터

2026-09-03 에 상품 페이지에서 확인한 것.

| 항목 | 확인한 사실 |
|---|---|
| 크기 | **50 × 70 cm** 원본 한 장 + **A3 축소판** 한 장 |
| 값 | 12,000원 (묶음 20,000원) |
| 만든 곳 | 디자인 스튜디오 203(이공삼). 월간 《스트리트H》 부록으로 매달 한 장 |
| 주제 | 한 장에 하나. 허준, 서울 지하철, 치킨, 장 담그기, 워크맨, 신문 제작 과정, 나무에서 종이까지 |
| 만드는 법(상품 설명 인용) | "마인드맵과 다이어그램 과정을 거쳐" 가운데에 주제의 핵심, 둘레에 역사·보조 정보를 놓아 "한눈에 쉽고 빠르게 이해될 수 있게" |
| 겉모습 | 그림이 많다. 글은 작고 촘촘하다. 어른 독자 기준 |

우리가 옮겨 올 것은 넷이다 — **한 주제**, **가운데 다이어그램 하나**, **둘레의 작은 칸**, **그림**.
다른 점은 하나다 — 독자가 **혼자 읽는 8~10세**다. 그래서 항목은 절반으로, 글자는 크게 간다.
A3 축소판을 함께 주는 방식은 이 저장소의 A3 인쇄물(`build/print-*.html`)과 짝이 맞는다.

## 내용 구성 원칙

1. **한 장 = 묶음서사 하나.** 인물 299·사건 263 을 다 넣을 수 없다. 데이터의 `arc` 가 스트리트H의 "한 주제"에
   해당한다. 한 장 분량은 **사건 10~18, 인물 20~40, 장소 5~12** 다. 그보다 크면 인물을 선별한다.
2. **가운데 다이어그램은 셋 중 하나다.** 순서가 핵인 이야기는 **원·띠**(열두 과업), 이동이 핵이면 **지도**(항해),
   관계가 핵이면 **나무**(계보). 하나만 고른다. 둘을 섞으면 어른용이 된다.
3. **둘레 칸의 재료는 이미 데이터에 있다.** `fun`(328개), 숫자(몇 해, 몇 명, 몇 곳), `modern`(지금의 지명),
   `symbols`·`domains`(신을 알아보는 표시), 로마 이름 한 줄. "찾아보기" 놀이(그림 속에서 메두사를 찾아라)도 여기 둔다.
4. **아이 기준 밀도.** 50cm 거리에서 본문 10pt 이상, 설명 7pt 이상. 읽는 순서를 번호로 준다.
   색은 `kind` 로 정한다(신/영웅/괴물/사람). 범례는 한 줄.
5. **수위와 톤은 집필-지침 그대로.** `sensitivity = "soften"` 항목은 `oneliner` 만 쓰거나 뺀다.
   교훈을 붙이지 않는다. `note` 는 그리지 않는다.
6. **이야기 속의 곳은 실제 지도에 찍지 않는다**([데이터-모델.md](데이터-모델.md) §2). 오디세우스의 열두 곳은 전부
   `mythic` 이라 **순서를 이은 그림 지도**로 그리고, 아이네이아스의 열네 곳 중 열둘은 `real` 이라 **실제 지도**에
   그린다. 뒷시대가 붙인 실제 위치는 `note` 에만 있고 포스터에 올리지 않는다.
7. **원전을 작게 적는다.** 아래 한 줄 "이 이야기가 적혀 있는 곳". 이설은 "다른 이야기도 있어" 작은 박스 하나.

## 구상안 여섯

숫자는 2026-09-03 의 `build/myth.json` 기준이다.

| | 제목 | 가운데 | 둘레 | 데이터 | 그림 재료 | 비고 |
|---|---|---|---|---|---|---|
| **A** | **헤라클레스의 열두 가지 일** | 시계 방향 **원 12칸**. 안쪽에 펠로폰네소스 작은 지도(네메아·레르나·라돈 강·에리만토스·엘리스·스팀팔로스 여섯이 한 반도에 있다), 바깥 고리에 먼 곳(크레타·트라키아·테미스키라·에리테이아·카우카소스·헤스페리데스·저승) | 위 띠: 탄생과 헤라의 미움(`herakles-early`, 9건). 아래 띠: 마지막과 신이 되다(`herakles-last`, 11건) | `herakles-labors` 사건 16(과업 12 + 폴로스·헤시오네·안타이오스·프로메테우스) · 인물 39 · 실제 장소 12 · 이야기 속 3 | 메트 CC0 도기 — Herakles 검색으로 이미지 있는 공개 도메인 유물 40건 확인 | **첫 장.** 자기 완결, 12 는 원이 되고, 그림이 가장 많다 |
| **B** | 아이네이아스의 바다 여행 | **실제 지도** 위 항로: 트로이→트라키아→델로스→크레타→스트로파데스→부트로툼→에트나→시켈리아→카르타고→쿠마이→아베르누스 | 아래 띠: 라티움의 싸움과 라비니움(`aineias-latium`, 9건). 구석: 저승·엘리시온은 "이야기 속의 곳" 칸 | `aineias-voyage` 17 · 인물 23 · 실제 12 · 이야기 속 2 | 도기 그림이 적다 → 19세기 판화 또는 생성 그림 | 지중해 전체가 한 장에 들어간다 |
| **C** | 오디세우스의 10년 | **이야기 지도** — 열두 곳(로토파고이→키클로페스→아이올리아→라이스트리고네스→아이아이아→저승→세이레네스→스킬라→트리나키아→오기기아→스케리아)을 순서로 이은 그림. 좌표 없음 | 아래 띠: 이타케의 귀환(`odysseus-homecoming`, 9건). 텔레마코스의 길은 작은 칸 | `odysseus-wanderings` 18 · 인물 25 · 실제 0 · 이야기 속 12 | 도기(폴리페모스·세이레네스는 많다) | "실제 위치를 모른다"를 아이에게 정직하게 보이는 첫 시도 |
| **D** | 트로이 전쟁, 누가 누구 편 | **두 진영의 인물 나무**(그리스 왼쪽, 트로이 오른쪽) + 가운데 10년 띠(파리스의 심판→목마) | 신들이 누구 편인지 한 줄씩 | `road-to-troy` 14 · `wrath-of-achilleus` 15 · `fall-of-troy` 8 · 인물 합 44+33+31 → **30명으로 선별** | 도기 풍부 | 인물이 많아 선별 규칙이 먼저 필요하다 |
| **E** | 신들의 계보 | **세대 띠 위의 나무** — 첫 신 10 → 티탄 21 → 올림포스 신. 12신은 **메달리온**(`symbols` 가 14명에 있다) | 로마 이름, 맡은 일(`domains`), "다른 이야기도 있어"(아프로디테의 두 탄생) | primordial 10 · titan 21 · god 23 | 상징물 아이콘 — 벡터 생성기(Recraft 등)에 가장 맞는 재료 | `print-family.html` 의 3단 나무를 포스터로 다시 짜는 것 |
| **F** | 아홉 시대 한눈에 | **A4 아홉 장 = 3×3 모듈.** 시대 하나가 A4 한 장. 흰 홈을 디자인으로 남겨 이음매를 맞추지 않는다 | 시대마다 5~8건 선별(지금 2·5·6·5·27·96·31·65·26 건) | 아홉 `era` 전부 | 시대별 대표 도판 한 점 | **집 프린터 전용 설계.** 한 장만 다시 뽑아 바꿀 수 있다. 인쇄 실험으로 좋다 |

순서는 **A → F → E → B** 를 권한다. A 는 첫 장으로 조건이 가장 좋고, F 는 인쇄 방법을 시험하면서 전체를 조망한다.
C 는 표현 문제(좌표 없음)를 풀어야 하고, D 는 선별 규칙이 먼저다.

## 그래픽 파이프라인 — 층으로 나눈다

"SVG 만으로는 한계가 있다"의 실체는 **그림**이다. 레이아웃·글자·다이어그램은 지금 방식이 가장 낫다.
그래서 층을 나눈다.

| 층 | 도구 | 되는 것 | 안 되는 것 |
|---|---|---|---|
| 1 판형·레이아웃·글자·다이어그램 | HTML/CSS/SVG → 크롬 `--print-to-pdf` | mm 단위 정확한 판형, 벡터, 폰트 내장, `myth.json` 연동. 지금 `render_print.py` 의 연장 | 삽화·질감 |
| 2 그림 (a) | **공개 도메인 도판** — 메트 Open Access(CC0, API `collectionapi.metmuseum.org/public/collection/v1/search?q=…&isPublicDomain=true&hasImages=true`), 위키미디어 공용, 클리블랜드·시카고 미술관(CC0) | 진짜 그리스 도기 그림. 값 없음, 표기 의무 없음(CC0). "원전에 없는 것을 쓰지 않는다"와 같은 태도 | 배경 제거·색 보정이 필요(Python `rembg`·Pillow, 선화는 `vtracer` 로 벡터화). 스타일을 통일하기 어렵다 |
| 2 그림 (b) | **외부 생성기** — Recraft(SVG 로 내준다), Adobe Illustrator Text to Vector(편집 가능한 벡터), Google Gemini 이미지, OpenAI gpt-image, Midjourney(스타일 참조), 로컬 Flux/Stable Diffusion(무료, GPU 필요) | 일관된 그림체, 원하는 장면 | **Anthropic 제품에는 이미지 생성이 없다.** 스크립트로 API 를 부르고 Claude 가 결과를 보고 고르는 흐름은 된다. 판매까지 가면 권리 검토 |
| 3 시안·손질 | **Claude Design**(`/design` 스킬, Pro/Max/Team/Enterprise) — 아트보드, 글자·간격·색 직접 손질, PDF/PNG/PPTX/HTML/Canva 내보내기, 이미지 업로드. **Figma 연결**(이 저장소의 세션에 연결기가 있다, 인증 필요) | 빠른 시안 여러 장, 손으로 미세 조정 | Claude Design 의 mm 판형·300dpi 사양은 문서에 없다 → 작은 시안으로 시험한 뒤 쓴다. Figma 의 인쇄 PDF 는 Figma 에서 뽑는다(MCP 는 편집용). 공식 `canvas-design` 스킬은 "글 적게·만화체 금지"가 원칙이라 맞지 않는다 |
| 4 검토 | Claude 비전 + A4 타일 시험 인쇄 | 렌더한 PNG 를 보고 고친다. 종이에서 글자 크기를 본다 | — |

그림 자산은 원전과 같이 다룬다 — `sources/images/` 에 파일을 두고 `data/images.toml` 에 출처·라이선스·묘사 인물을
적는다(아래). 저장소에 넣기 전에 라이선스를 본다. 메트는 CC0 이고, 대영박물관은 CC BY-NC-SA 라 판매용에는 못 쓴다.

폰트는 본문 Pretendard 또는 Noto Sans KR(둘 다 OFL), 제목용은 따로 고르고 라이선스를 확인한다. PDF 에 내장한다.
색은 sRGB 로 디자인하고, CMYK 로 안 나오는 형광·진한 청록은 피한다. 시험 인쇄로 확인한다.

## 인쇄

### 판형과 해상도

| 판형 | mm | 비고 |
|---|---|---|
| A4 | 210 × 297 | 집 프린터 |
| **A4 2×2 = A2** | **420 × 594** | 정확히 A2 다. 겹침 5~10mm 를 두면 프린터 여백이 덮인다 |
| A4 3×3 | 630 × 891 | 스트리트H(500×700)보다 조금 크다. 구상안 F |
| 스트리트H | 500 × 700 | 전문 출력 시 그대로 가능(레드프린팅 최대 500×730) |
| A1 | 594 × 841 | |

그림 해상도는 A2 300dpi 가 4961×7016px, 150dpi 가 2480×3508px 다. 벽에 붙이는 포스터는 150dpi 로도 충분하다.
벡터 PDF 면 글자·선은 해상도와 무관하고, 그림만 해당한다.

### 집에서 — A4 컬러 프린터

- **Adobe Acrobat Reader 의 "포스터" 인쇄.** 인쇄 창 → 페이지 크기 조정 → 포스터. 배율 100%, 겹침 5~10mm,
  재단선·라벨을 켠다. A2 는 4장, 630×891 은 9장. 드라이버와 무관하게 된다.
- **PosteRazor**(무료·오픈소스, 브라우저판도 있다) — 래스터 그림을 여러 장 PDF 로 자른다. 프린터 드라이버의
  "포스터 2×2" 도 같은 일이다.
- **종이.** 120~180g 무광 잉크젯 용지. 광택지는 이음매가 보인다.
- **조립.** 재단선을 따라 각 장의 한 변만 자르고, 겹침 부분을 딱풀이나 양면테이프로 붙인다. 5mm 우드락에 붙이면 선다.
- **모듈형(F).** 이음매를 맞추지 않는다. 홈이 디자인이다. 한 장 틀리면 그 장만 다시 뽑는다.

### 전문 출력 — 1장도 된다

- **레드프린팅 종이 포스터**(2026-09-03 확인): 1장부터. A2 는 재단 여유 포함 424×598mm. **PDF, 300dpi 이상,
  사방 2mm 재단 여유, 재단선 안쪽 3mm 안전 영역.** A3 초과는 120g 이상. 당일(11시)·익일(17시) 출발이 있다.
  **가격은 설정 화면에서만 보여 확인하지 못했다.**
- 그 밖: 100부닷컴·포스터메이커스(1장 소량), 킨코스(방문, 대형 출력). A3 는 편의점·킨코스에서 바로 뽑힌다.

**권하는 흐름:** 집에서 A4 타일로 시안을 여러 번 뽑아 글자 크기와 밀도를 맞추고, 확정된 한 장만 전문 출력한다.

## 데이터·코드 제안

```toml
# data/posters/herakles-labors.toml
[[poster]]
id = "herakles-labors"
title = "헤라클레스의 열두 가지 일"
size_mm = [420, 594]            # 또는 [500, 700]
arcs = ["herakles-labors"]
center = { kind = "ring", items = ["nemean-lion-slain", "lernaean-hydra-slain", "..."] }   # ring | map | tree | band
[[poster.side]]
label = "태어나서"
items = ["herakles-born", "..."]            # herakles-early 에서 고른 것
[[poster.side]]
label = "재밌는 것"
fun_from = ["herakles", "kerberos", "hydra"]  # 이 항목들의 fun 을 쓴다
images = ["met-herakles-lion-1", "..."]
```

```toml
# data/images.toml — 원전 목록(sources.toml)과 같은 태도
[[image]]
id = "met-herakles-lion-1"
file = "sources/images/met-12345.jpg"
source = "The Metropolitan Museum of Art, objectID 12345"
url = "https://www.metmuseum.org/art/collection/search/12345"
license = "CC0"
depicts = ["herakles", "nemean-lion"]        # figure id. 빌드가 검사한다
note = "배경 제거함(rembg). 흑색상 암포라, 기원전 6세기"
```

- `tools/build.py` 가 `posters`·`images` 를 읽어 **없는 id 면 즉시 죽는다**(규칙 2). `depicts` 의 figure 도 검사한다.
- `tools/render_poster.py` 가 `build/myth.json` 만 읽어 `build/poster-<id>.pdf` 와 미리보기 PNG 를 만든다.
  판형은 CSS `@page { size: 420mm 594mm }`, 전문 출력용은 2mm 여유를 더한 판을 따로 낸다.
- 포스터에 쓰는 문장은 데이터의 `oneliner`·`fun`·`modern` 이다. 렌더러가 새 문장을 만들지 않는다.

## 진행 단계

0. **결정.** 판형(A2 = A4 넷 / 50×70), 첫 장 A, 그림 노선(도판 먼저 시도, 부족하면 생성).
1. **내용 명세.** A 의 마인드맵 한 장 — 넣을 id, 둘레 칸, 읽는 순서 → `data/posters/herakles-labors.toml`.
2. **무그림 레이아웃.** `render_poster.py` 첫 판 → 크롬 PDF → **A4 4장 타일 시험 인쇄** → 글자 크기·밀도 확정.
3. **그림.** 메트 API 로 헤라클레스 도기 수집 → 배경 제거 → `data/images.toml`. 도판 그대로 쓸지, 선화로
   벡터화해 그림체를 통일할지 여기서 정한다. HANDOFF 의 "그림 자료 조사"가 이 단계다.
4. **확정.** 300dpi·2mm 여유 PDF → 전문 출력 1장 → 벽에 붙이고 아이의 반응을 본다.
5. **확장.** F(3×3 모듈, 집 프린터) → E → B. 렌더러는 `center.kind` 별로 하나씩 늘린다.

## 미정·확인할 것

- Claude Design 의 인쇄 사양(mm 판형, 내보내기 해상도). 작은 시안으로 시험한다.
- 1장 전문 출력 가격(레드프린팅 등 설정 화면).
- 생성 그림의 권리(판매 시). 도판 가운데 NC 라이선스(대영박물관) 구분.
- 제목 폰트 라이선스.
- C 의 "이야기 지도" — 실제 위치가 없다는 것을 아이에게 어떻게 보일지.

## 조사 출처 (2026-09-03)

- 스트리트H — [인포그래픽 포스터 목록](https://street-h.com/product/product-category/infographic-poster/) ·
  [서울 지하철](https://street-h.com/product/112349/) · [허준](https://street-h.com/product/111619/)
- Anthropic — [Introducing Claude Design](https://www.anthropic.com/news/claude-design-anthropic-labs) ·
  [Claude Design 제품 페이지](https://claude.com/product/design) · [Claude Code artifacts](https://code.claude.com/docs/en/artifacts) ·
  [canvas-design 스킬](https://github.com/anthropics/skills/blob/main/skills/canvas-design/SKILL.md) ·
  [비전(이미지 입력) 문서](https://platform.claude.com/docs/en/build-with-claude/vision)
- Figma — [Claude Code 와 Figma MCP 설정](https://help.figma.com/hc/en-us/articles/39888612464151-Claude-Code-and-Figma-Set-up-the-MCP-server) ·
  [Claude Code to Figma](https://www.figma.com/blog/introducing-claude-code-to-figma/)
- 인쇄 — [Acrobat 큰 문서 인쇄](https://helpx.adobe.com/acrobat/desktop/print-documents/set-up-and-print-pdfs/custom-sizes.html) ·
  [Acrobat Reader 포스터·배너](https://helpx.adobe.com/ph_fil/acrobat/kb/print-posters-banners-acrobat-reader.html) ·
  [PosteRazor](https://posterazor.sourceforge.io/) · [레드프린팅 종이 포스터](https://www.redprinting.co.kr/ko/product/item/PR/PRPOXXX) ·
  [포스터메이커스](https://postermakers.co.kr/category0/?idx=271) · [100부닷컴 A2](https://baekboo.com/product/a2-%ED%8F%AC%EC%8A%A4%ED%84%B0%EC%9D%B8%EC%87%84-%ED%8F%AC%EC%8A%A4%ED%84%B0%EC%B6%9C%EB%A0%A5-%EC%86%8C%EB%9F%89%EC%9D%B8%EC%87%84/149)
- 도판 — [The Met Open Access](https://www.metmuseum.org/about-the-met/policies-and-documents/open-access) ·
  [Met Collection API (Herakles 검색 예)](https://collectionapi.metmuseum.org/public/collection/v1/search?q=Herakles&isPublicDomain=true&hasImages=true) ·
  [위키미디어 재단: Met CC0](https://wikimediafoundation.org/news/2017/02/07/the-met-public-art-creative-commons/)
