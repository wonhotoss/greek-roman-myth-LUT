# 인수인계 — greek-roman-myth-LUT

이 문서는 **이 저장소를 처음 여는 사람**(사람이든 에이전트든)을 위한 것이다.
무엇이 왜 이렇게 되어 있는지, 무엇을 건드리면 안 되는지, 다음에 무엇을 할지를 적는다.
프로젝트 자체의 설명은 [README.md](README.md), 스키마는 [데이터-모델.md](데이터-모델.md) 에 있다.

## 한 문단 요약

혼자 읽는 8~10세 아이가 그리스 로마 신화를 **시간순·지리·인물·사건**으로 찾아보게 하는 자료다.
만들 형태가 셋(기기 탐색 웹 / 벽에 붙이는 인쇄물 / 음성 에이전트)이라 **어느 것도 먼저 만들지 않고**
`data/*.toml` 한 벌을 먼저 만들었다. 빌드가 이것을 `build/myth.json` 하나로 합치고, 렌더러 셋이
같은 JSON에서 세 산출물을 뽑는다. 지금 아홉 시대가 모두 채워져 있다 —
**인물 299 · 사건 263 · 장소 83 · 묶음서사 33 · 원전 13.**

## 이력

- 2026-09-02 `wonhotoss/scratch` 인큐베이터의 `greek-roman-myth/` 로 착수. 스키마·집필 지침 확정,
  era 0~4 슬라이스로 산출물 셋이 실제로 나오는지 확인.
- 2026-09-02 사건을 점에서 **구간**으로 바꿈. 좌표를 데이터에 적지 않고 인과 제약을 빌드가 푼다.
- 2026-09-03 era 5~8 집필로 아홉 시대를 모두 덮음. 같은 날 상설 저장소로 분리 —
  **이 저장소가 그것이다.** 그 전 이력은 `scratch` 저장소의 `greek-roman-myth/` 경로에 남아 있다
  (마지막 커밋 `d3f7a65`).

## 5분 만에 돌려 보기

```sh
py -3.13 tools/build.py          # data/*.toml 검증 → build/myth.json
py -3.13 tools/render_web.py     # → build/myth.html      브라우저로 열면 끝
py -3.13 tools/render_print.py   # → build/print-*.html   브라우저에서 인쇄/PDF
py -3.13 tools/render_agent.py   # → build/agent-pack/
```

의존성 없음. 표준 라이브러리 `tomllib` 만 쓴다.

> **파이썬 주의.** 이 PC의 Git Bash `python` 은 `C:\Python33`(3.3.0)으로 잡혀 f-string 에서 죽는다.
> `py -3.13` 또는 `/c/Python313/python.exe` 를 쓴다.

`build/` 는 생성물이지만 **커밋한다.** 자료를 받는 사람이 파이썬 없이 `build/myth.html` 하나만
열어도 되게 하려는 것이다. 데이터를 고치면 렌더러 셋을 다시 돌려 함께 커밋한다.

## 이 프로젝트의 규칙 — 어기면 자료가 못 쓰게 된다

1. **데이터가 먼저다.** 렌더러는 `build/myth.json` 을 읽기만 한다. 화면에 필요한 값을
   렌더러 안에서 만들어 쓰면 인쇄물·에이전트 팩에서 사라진다.
2. **빌드는 깨지면 즉시 죽는다.** 기본값을 채우거나 없는 참조를 건너뛰지 않는다.
   틀린 데이터로 산출물이 나오는 것이 최악이다. 이 성질을 약화시키지 않는다.
3. **원전에 없는 것을 쓰지 않는다.** 항목마다 `sources` 를 달고, 인용 위치는 `sources/` 원문에서
   실제로 찾아 확인한다. 못 찾으면 항목을 쓰지 않는다.
4. **모르는 것을 아는 것처럼 그리지 않는다.** 순서를 모르면 모른다고 남긴다
   ([불확실-목록.md](불확실-목록.md)). 빌드가 자동 점검을 만든다.
5. **`id` 는 절대 바꾸지 않는다.** 링크 대상이고 주소(`#지도:medousa`)에 들어간다.
6. **파생 값을 데이터에 두 번 적지 않는다.** `children`(부모의 역인덱스), `event.arc`
   (묶음의 역인덱스), 시간축 좌표는 모두 빌드가 만든다. 예전에 `arc` 를 양쪽에 적었다가
   한 사건이 두 줄에 그려지는 버그가 났다.
7. **수위 판단은 [집필-지침.md](집필-지침.md) 의 표를 따른다.** 기준은 하나 —
   "이 장면을 빼면 다음 사건이 설명되지 않는가". 빼면 무너지는 것은 사실만 짧게 쓰고
   `sensitivity = "soften"` 을 붙인다. 판단을 사람마다 다르게 하면 자료가 흔들린다.
8. **신화를 도덕 교훈으로 바꾸지 않는다.** 신들은 착하지 않다. 판단은 아이가 한다.

## 항목을 하나 더 쓰려면

1. [데이터-모델.md](데이터-모델.md) 의 스키마와 [집필-지침.md](집필-지침.md) 의 문장 규칙을 읽는다.
2. 같은 성격의 기존 파일을 표본으로 삼는다(예: 영웅 서사는 `data/events/era5-herakles.toml`).
3. 원전 구절을 `sources/` 에서 grep 으로 찾아 인용 위치를 확인한다.
   아폴로도로스는 파일 안에 `[2.5.1]`, 리비우스는 `4. `, 헤시오도스는 `(ll. 116-138)` 표시가 그대로 있다.
4. 사건이면 `era` 안에서 **`seq` 가 겹치지 않게** 잡고(10 간격), 이야기 안의 순서는
   `caused_by`/`after` 로 **함께** 적는다. 둘 다 적어야 자동 점검이 "순서를 안다"로 본다.
5. 묶음서사에 넣는다(`data/arcs.toml`). 한 사건은 한 묶음에만 든다.
6. 빌드 → 렌더러 셋을 돌리고, `build/불확실-점검.md` 가 늘지 않았는지 본다.

## 지금 상태에서 알아야 할 것

- **원전 코퍼스는 전부 커밋되어 있다**(`sources/`, 12MB). 매번 다시 받지 않기 위한 것이고,
  재현 절차는 README에 있다. 전부 공개 도메인 번역이다.
- **`sources/hyginus-fabulae-*.txt` 는 발췌다.** theoi.com 추출이 잘려 우화 1~4·50~55·100~103·
  150~155·200~203 만 들어 있다. 그래서 이설의 주 출처로 쓰지 못했다(인용 11건). 다시 받아야 한다.
- **참고 도서** 미래엔아이세움 『처음 읽는 그리스 로마 신화』(최설희 글, 전 15권)는 **범위와 순서의
  기준**으로만 썼다. 어린이용 현대 재화이므로 `sources` 에 인용하지 않는다.
  대응은 항목 `note` 의 "참고: N권 M장 「소제목」" 으로만 남아 있다.
- **아직 항목이 없어 이름만 부르는 인물이 백여 명이다**(네레우스·칼리오페·리코메데스 등).
  이야기에 필요한 만큼만 만들었다. 필요해지면 그때 만든다.
- **`sensitivity` 와 `note` 는 화면에 그리지 않는다.** 아이가 보는 것이다.
  에이전트 팩에는 준다 — 무엇을 말하지 않을지 알아야 하기 때문이다.

## 다음에 할 일 (우선순위)

1. **그림 자료 조사.** 8~10세에게 그림 없는 신화는 절반이다. 공개 도메인 도판(그리스 도기 사진·
   19세기 판화)의 출처·라이선스·해상도를 조사해 `sources/` 에 모으고 항목에 붙인다.
   가장 크게 빠져 있는 조각이다.
2. **대화 팩 실측.** `my-talking-claw`(브라우저 STT → `claude -p` → TTS)에 얹어 아이의 실제
   질문으로 시험한다. 지식팩이 874KB 로 커져 색인으로 필요한 파일만 열게 하는 것이 함께 필요하다.
3. **`place_unknown` 넣기.** 자동 점검의 장소 목록에서 "원전이 말하지 않는 것"과 "아직 안 넣은 것"을
   가른다([불확실-목록.md](불확실-목록.md) D-3). 손이 가장 적게 가고 잡음을 가장 많이 줄인다.
4. **히기누스 원문 다시 받기.**
5. **남은 장소 다섯**(리디아·리비아·오이칼리아)을 항목으로 만들지 정한다.

손대지 않은 것: 접근성(글자 크기 조절·화면 낭독), 계보 나무에서 부모가 둘인 인물의 중복 표시.

## 검사 도구

빌드가 스키마·참조·인과 순환·`seq` 충돌을 전부 본다. 그 밖에 이번 집필에서 쓴 검사는
저장소에 넣지 않았다(일회성이었다). 다시 필요하면 이런 것들이었다.

- 인용 대조 — `sources` 의 아폴로도로스·리비우스 위치가 원문에 실제로 있는지. 737건 전건 확인했다.
- 문체 — `oneliner` 한 문장, `body` 3~5문장, 문장 40자. 다만 **기존 자료도 이 한계를 느슨하게
  적용해 왔다**(아테나 60자, 키클롭스 56자). 새로 쓸 때 그 수준에 맞추면 된다.
- 이름 겹침 — `name_ko` 가 겹치는 항목. 지금 겹치는 셋(타르타로스·오케아노스·아르고스)은
  **의도한 것이다** — 신이면서 장소이거나, 개와 도시다.

## 저장소

`git init` 만 해 두었고 원격은 붙이지 않았다. 푸시는 저장소 주인이 한다.
`.gitignore` 는 파이썬 부산물과 Claude Code 로컬 설정만 뺀다 — **`sources/` 와 `build/` 는 커밋한다.**
